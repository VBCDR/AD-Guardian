<Window x:Class="Domain_Guardian.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About AD Guardian"
        Width="480"
        Height="360"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid Background="#22000000"/>

        <Border Background="White" CornerRadius="14" Padding="24" BorderBrush="#D7E2EC" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.3" Color="Black"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <Image Width="28" Height="28" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Image.Source>
                            <BitmapImage UriSource="pack://application:,,,/AD-Guardian-logo-_2_.ico" DecodePixelWidth="28" DecodePixelHeight="28" CacheOption="OnLoad"/>
                        </Image.Source>
                    </Image>
                    <TextBlock Text="AD Guardian"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="#17324D"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="VersionText"
                               FontSize="13"
                               Foreground="#7A8B9F"
                               VerticalAlignment="Center"
                               Margin="10,2,0,0"/>
                </StackPanel>

                <TextBlock Grid.Row="1"
                           Text="Active Directory diagnostics, scheduling, log export, and historical run review."
                           TextWrapping="Wrap"
                           Foreground="#4A5B6C"
                           Margin="0,0,0,14"/>

                <Border Grid.Row="2"
                        Background="#F5F8FC"
                        CornerRadius="8"
                        Padding="14,10"
                        Margin="0,0,0,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Version" FontWeight="SemiBold" Foreground="#4A5B6C" FontSize="13"/>
                        <TextBlock Grid.Column="1" x:Name="VersionValueText" Foreground="#17324D" FontSize="13"/>
                        <TextBlock Grid.Row="1" Text="Framework" FontWeight="SemiBold" Foreground="#4A5B6C" FontSize="13" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="FrameworkText" Foreground="#17324D" FontSize="13" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="2" Text="Project" FontWeight="SemiBold" Foreground="#4A5B6C" FontSize="13" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,4,0,0">
                            <Hyperlink NavigateUri="https://github.com/CianRogers/AD-Guardian" RequestNavigate="Hyperlink_RequestNavigate" FontSize="13">
                                github.com/CianRogers/AD-Guardian
                            </Hyperlink>
                        </TextBlock>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Check for Updates"
                            Margin="0,0,10,0"
                            Padding="16,9"
                            Background="#1A73E8"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="ManualUpdateButton_Click"/>
                    <Button Content="Close"
                            Padding="16,9"
                            Background="#E0E6ED"
                            Foreground="#17324D"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
